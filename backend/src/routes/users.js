const router = require('express').Router();
const db = require('../config/database');
const logger = require('../config/logger');

// TODO: Add user profile routes here (e.g., GET /me, PATCH /profile)

module.exports = router;
